#include <iostream>
#include <algorithm>
#include <cstring>
#include <map>

typedef long long ll;
typedef std::pair<int, int> pii;

const int N = 2e5 + 5;

int n;
int a[N];

void solve() {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int t = 1;
    std::cin >> t;
    while (t -- ) solve();

    return 0;
}