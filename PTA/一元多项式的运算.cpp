#include<iostream>
using namespace std;

struct item{
    int coe;
    int exp;
    item* next;
};

int main()
{
    item* head = new item;
    item* q = head;
    int n;
    cin >> n;
    for(int i = 0;i < n; i++){
        item* temp = new item;
        
        cin >> 
    }
    return 0;
}