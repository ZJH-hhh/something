#include <iostream>
#include <algorithm>
#include <cstring>

using namespace std;

const int mod = 998244353;

void solve(){
    string s;
    int k;
    cin >> s >> k;
    s = '0' + s;

    int res = 0;
    for (int i = 1; i < s.size(); i ++ )
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int t = 1;
    cin >> t;
    while (t -- ) solve();

    return 0;
}