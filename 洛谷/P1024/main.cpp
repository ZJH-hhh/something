#include <iostream>
#include <algorithm>
#include <cstring>

int main() {
    double a, b, c, d;
    std::cin >> a >> b >> c >> d;

    int cnt = 0;
    for (int i = -100; i < 100; i ++ ) {
        int l = i, r = i + 1;

    }
}